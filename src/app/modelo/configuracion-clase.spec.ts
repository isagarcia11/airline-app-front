import { ConfiguracionClase } from './configuracion-clase';

describe('ConfiguracionClase', () => {
  it('should create an instance', () => {
    expect(new ConfiguracionClase()).toBeTruthy();
  });
});
